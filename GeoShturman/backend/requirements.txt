# ---------------------------------------------------------------------------
# Зависимости бэкенда.
#
# Этот файл сопровождают Агент 1 (математическое ядро) и Агент 2 (API).
# Ниже — минимальный набор, достаточный для сборки образа и запуска API/статики.
# При объединении ветвей выровняйте версии под фактические нужды ядра.
# ---------------------------------------------------------------------------

# Веб-слой
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# Математическое ядро / визуализация (Агенты 1 и 2)
numpy==2.2.1
scipy==1.15.0
matplotlib==3.10.0
pillow==11.1.0

# Tests / local verification
pytest==9.1.1
httpx==0.28.1
