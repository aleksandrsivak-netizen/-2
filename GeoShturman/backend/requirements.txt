# ---------------------------------------------------------------------------
# Зависимости backend, API, визуализации и алгоритмического слоя.
# Версии закреплены для воспроизводимого локального запуска и сборки образа.
# ---------------------------------------------------------------------------

# Веб-слой
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# Математическое ядро / визуализация
numpy==2.2.1
scipy==1.15.0
matplotlib==3.10.0
pillow==11.1.0

# Реальный DEM (Copernicus GLO-30 через GDAL/vsicurl) — опционально, DEM_SOURCE=real
rasterio==1.5.0
pyproj==3.7.2

# Tests / local verification
pytest==9.1.1
httpx==0.28.1
